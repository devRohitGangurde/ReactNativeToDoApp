/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ToDoListScreen from './src/screens/ToDoListScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ToDoListScreen);
